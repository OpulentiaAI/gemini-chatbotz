# Task completion checklist
- Run `pnpm lint` if TS/React changes.
- Run relevant tests: `pnpm test` or targeted spec if applicable.
- If Convex changes: `pnpm convex:dev` locally; `pnpm convex:deploy` for prod.
- Verify `pnpm dev` or `pnpm start` if UI changes.